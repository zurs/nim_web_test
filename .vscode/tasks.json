{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run nim_web",
            "type": "shell",
            "command": "nim c -r -o:bin/nim_web --threads:On src/nim_web.nim",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}